export default function SearchBar(props) {
  const onSearch = () => {
    
  };

  return (
    <div>
      <input type = 'search'/>
      <button onClick={onSearch}>Buscar</button> 
    </div>
  );
}
